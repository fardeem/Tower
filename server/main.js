import { Meteor } from 'meteor/meteor';

import '../imports/api/models/subjects.js';
import '../imports/api/models/teachers.js';
import '../imports/api/models/exams.js';

Meteor.startup(() => {
  // code to run on server at startup
});
